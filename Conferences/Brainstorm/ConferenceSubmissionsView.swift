import SwiftUI
/*
struct ConferenceSubmissionsView: View {
    let set: NSSet
    
    var body: some View {
        Section("Submitted to") {
            ForEach(set.allObjects) {
                Text(set.description)
                //Text(conferences[index].name)
            }
        }
    }
}
*/
